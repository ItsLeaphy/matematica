function toggle(id) {
  const el = document.getElementById(id);

  if (el.style.display === "none") {
    el.style.display = "block";
  } else {
    el.style.display = "none";
  }
}

function toggle(id) {
  const el = document.getElementById(id);

  if (el.style.display === "none") {
    el.style.display = "block";
  } else {
    el.style.display = "none";
  }
}

function esconderTodos(classe) {
  const elementos = document.querySelectorAll(classe);

  elementos.forEach(el => {
    el.style.display = "none";
  });
}

function esconderTodos(classe) {
  const elementos = document.querySelectorAll(classe);

  elementos.forEach(el => {
    el.style.display = "none";
  });
}